#include "main.h"

/**
 * _printf - function
 * @char: character
 * @format: is a character string
 * @s:
 */
int _printf(const char *format, ...)
{
	char *str = va_arg(types, char *);

	if (str == NULL)
	{
		str = "(null)";
	}
}

/**print %**/


